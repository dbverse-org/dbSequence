% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pool.R
\name{pool}
\alias{pool}
\alias{pool.default}
\alias{pool.tbl_duckdb_connection}
\alias{pool.tbl_dbi}
\alias{pool.dbSequence}
\title{Pool (aggregate) values by grouping columns}
\usage{
pool(
  x,
  group_by,
  value_col = NULL,
  fun = "sum",
  name = NULL,
  filter_zero = TRUE,
  temporary = TRUE,
  overwrite = TRUE,
  ...
)

\method{pool}{default}(
  x,
  group_by,
  value_col = NULL,
  fun = "sum",
  name = NULL,
  filter_zero = TRUE,
  temporary = TRUE,
  overwrite = TRUE,
  ...
)

\method{pool}{tbl_duckdb_connection}(
  x,
  group_by,
  value_col = "x",
  fun = "sum",
  name = NULL,
  filter_zero = TRUE,
  temporary = TRUE,
  overwrite = TRUE,
  ...
)

\method{pool}{tbl_dbi}(
  x,
  group_by,
  value_col = "x",
  fun = "sum",
  name = NULL,
  filter_zero = TRUE,
  temporary = TRUE,
  overwrite = TRUE,
  ...
)

\method{pool}{dbSequence}(
  x,
  group_by,
  value_col = "score",
  fun = "sum",
  name = NULL,
  filter_zero = TRUE,
  temporary = TRUE,
  overwrite = TRUE,
  ...
)
}
\arguments{
\item{x}{A dbSequence object, tbl_duckdb_connection, or other lazy table}

\item{group_by}{Character vector of column names to group by}

\item{value_col}{Character, the column to aggregate. Default is "score"
for dbSequence, "x" for tbl objects.}

\item{fun}{Character, aggregation function: "sum" (default), "mean",
"count", "min", "max"}

\item{name}{Character, optional name for the resulting computed table.
If NULL, returns a lazy query without materializing.}

\item{filter_zero}{Logical, if TRUE (default) filter out rows where
aggregated value == 0}

\item{temporary}{Logical, if TRUE (default) create a temporary table,
otherwise create a permanent table}

\item{overwrite}{Logical, if TRUE (default) overwrite existing table}

\item{...}{Additional arguments passed to dplyr::compute()}
}
\value{
Same type as input (lazy, still in database). For dbSequence,
returns dbSequence if range columns are preserved, otherwise returns tbl.
}
\description{
Aggregates values in a lazy database table by grouping columns. This is
the database-native equivalent of \code{dplyr::group_by() + summarise()} but
with a simpler interface optimized for common pooling operations.
}
\examples{
bed <- system.file("extdata", "example.bed", package = "dbSequence")
db_seq <- read_bed(bed)

# Pool feature scores by feature name
pooled <- pool(db_seq, group_by = "name", value_col = "score")
pooled

}

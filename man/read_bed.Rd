% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plyranges-compat.R
\name{read_bed}
\alias{read_bed}
\title{Read a BED file into DuckDB}
\usage{
read_bed(
  file,
  dest = DuckDBFile(":memory:"),
  table_name = "bed_data",
  lazy = TRUE,
  ...
)
}
\arguments{
\item{file}{Path to BED file}

\item{dest}{DuckDBFile or path: destination database (default: in-memory)}

\item{table_name}{character: name for the table (default: "bed_data")}

\item{lazy}{logical: if TRUE (default), do not import/copy the file into a
DuckDB table. Instead, return a dbSequence backed by a DuckDB scan query
(an inline SQL SELECT over the file). Call \code{dplyr::compute()} on the
result to materialize when needed.}

\item{...}{Additional arguments passed to import()}
}
\value{
A dbSequence object
}
\description{
plyranges-style function to read BED files. Unlike the
plyranges version which returns GRanges, this returns a dbSequence
object backed by DuckDB for lazy evaluation.
}
\examples{
\dontrun{
# Read BED file (lazy, stays in DuckDB)
db_seq <- read_bed("regions.bed")

# Filter before collecting
db_seq \%>\%
  filter(score > 100) \%>\%
  as_granges()
}

}
\seealso{
\code{\link[BiocIO]{import}}, \code{\link[rtracklayer]{BEDFile}}
}
